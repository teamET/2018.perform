
server{
  listen 80;
  server_name localhost;

  access_log /root/logs/access.log;
  error_log /root/logs/error.log;

  location / {
    root /root/public;
  }

  location /api {
    proxy_pass https://express:8080;
  }

}
