# 2018.perform

```
docker-compose build
docker-compose up
```
## then you can view at localhost
##  just do it

you can edit files  
top page -> ` ./static/ `  
api server -> ` ./api-server/app.js `
